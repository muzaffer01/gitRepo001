# AI Prompts

1. Build a web application like Amazon, with three pages for now: a List Page for products, a Details page for a product, and a Cart Page. Build the application, test it, store all code locally, integrate with the user's Git account and push the code to GitHub, and create documents in Google Drive (Test Plan, Test Cases, Test Run Reports, Product Requirements Document, Technical Design Document). Treat this as a test environment and run in auto mode (no clarifying/confirmation questions) until the goals are met.
2. Choose the tech stack for a small multi-page web app prototype like this: React + Vite, react-router-dom for routing, React Context + localStorage for cart state, Vitest + React Testing Library for unit/component tests, mock local JSON/JS product data (no backend).
3. Also commit the standard documentation set into the Git repo's `docs/` folder, not just Google Drive, and maintain a Runbook (in both Git and Drive) capturing the full requirements, operating preferences, and step-by-step build procedure, kept up to date, so the same project can be recreated end-to-end in auto mode on request.
4. Was Playwright used for testing? Add Playwright for automated end-to-end browser testing, alongside the existing Vitest/RTL unit tests, and update the documentation set (TDD, Test Plan, Test Cases, Test Run Report) to reflect the new layer.
5. Were the existing tests TDD or BDD style? Add a separate Cucumber-based BDD test suite (Given/When/Then Gherkin, driven by Playwright), covering the same behaviors as the existing unit ("TDD"-labeled) suite and the E2E suite — full parity, not a subset, with a cross-reference table mapping every case across suites.
6. Demonstrate the failure-reporting workflow: create a deliberately failing test scenario using a real (not fabricated) defect, isolated from the main test suite and build so CI stays green, with a full defect report (status, severity, repro steps, root cause, suggested fix, evidence) and a dedicated Test Run Report for the failing run. Leave the defect open on purpose — the point is to demonstrate the reporting workflow, not close the bug.
7. Every document's "Author" (or "Found by") field must say Muzaffer, never "Claude" — apply this retroactively to every existing document in both Git and Drive, not just new ones going forward.
8. Add a "Created using the Claude agent running in Windows Terminal" note to every document, since the entire project (app, tests, GitHub integration, documentation) was built through that terminal-based agent session specifically.
9. Package each phase of the build/test/publish procedure as a standalone, reusable Claude Code Skill under `.claude/skills/` (environment check, scaffold, add-E2E, add-BDD, verify, publish-to-GitHub, publish-docs, demo-failing-test), each linking back to this repo's actual files as a worked example, plus a visual flow diagram showing how the skills chain together.
10. (Standing rule, all projects) Always create new projects under `C:\Users\muzzu\Desktop\Muz_AI_Projects_win` — never directly under Desktop or elsewhere — even if a project's own prompt/intent document names a different path; this project was originally scaffolded directly under Desktop and had to be moved to match, once the rule was made explicit.
11. (Standing rule, all projects) For any Test Case document, always create two separate documents — never one merged file — named `TestCases-TDD.md` (imperative unit/E2E/manual cases) and `TestCases-BDD.md` (Given/When/Then Gherkin scenarios quoted verbatim from the `.feature` files), cross-linked to each other. In the TDD document's failing/known-defect row, always link directly to the defect report file, not just in surrounding prose.
12. For this project specifically, create a Requirements Traceability Matrix (RTM) mapping every PRD requirement forward to its TDD/BDD test case(s) and back to any linked defect, with an honest coverage-gap summary, and a Defect Summary Report rolling up every defect (register table + per-defect detail + cross-defect observations) as a companion to the individual defect reports.
